server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jhavierc/spring-cloud-configServer-openFeign
          force-pull: true
          search-paths: service-configuration
          default-label: main
